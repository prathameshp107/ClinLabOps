'use client'import { useState } from 'react'import { Button } from '@/components/ui/button'import { Icons } from '@/components/icons'import { NavItems } from '@/components/nav-items'import { cn } from '@/lib/utils'  className?: string}export function Sidebar({ className }) {  const [isCollapsed, setIsCollapsed] = useState(false)  return (    <div      className={cn(        'relative hidden h-[calc(100vh-4rem)] flex-col border-r bg-background md:flex',        isCollapsed ? 'w-16' : 'w-64',        className      )}    >      <div className="flex-1 overflow-y-auto">        <div className="flex h-16 items-center justify-center border-b p-4">          {!isCollapsed && (            <div className="flex items-center space-x-2">              <Icons.logo className="h-6 w-6" />              <span className="font-semibold">LabTasker</span>            </div>          )}          {isCollapsed && <Icons.logo className="h-6 w-6" />}        </div>        <div className="p-4">          <NavItems isCollapsed={isCollapsed} />        </div>      </div>      <div className="border-t p-4">        <Button          variant="ghost"          size="sm"          className="w-full justify-start"          onClick={() => setIsCollapsed(!isCollapsed)}        >          <Icons.chevronLeft            className={cn(              'h-4 w-4 transition-transform',              isCollapsed && 'rotate-180'            )}          />          {!isCollapsed && <span className="ml-2">Collapse</span>}        </Button>      </div>    </div>  )}