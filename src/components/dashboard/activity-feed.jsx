import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'import { Avatar, AvatarFallback, AvatarImage } from '@/components/ui/avatar'import { Icons } from '@/components/icons'const activities = [  {    id: '1',    user: {      name: 'John Doe',      avatar: '/avatars/01.png',    },    action: 'completed',    target: 'task',    targetTitle: 'Design new dashboard layout',    time: '2 minutes ago',    project: 'Website Redesign',  },  {    id: '2',    user: {      name: 'Jane Smith',      avatar: '/avatars/02.png',    },    action: 'commented',    target: 'task',    targetTitle: 'API documentation',    time: '1 hour ago',    project: 'API Development',    comment: 'I\'ve reviewed the documentation. Looks good overall, just a few minor suggestions.',  },  {    id: '3',    user: {      name: 'Alex Johnson',      avatar: '/avatars/03.png',    },    action: 'assigned',    target: 'task',    targetTitle: 'Fix login page layout',    time: '3 hours ago',    project: 'Bug Fixes',    assignedTo: 'John Doe',  },  {    id: '4',    user: {      name: 'Mike Wilson',      avatar: '/avatars/04.png',    },    action: 'created',    target: 'project',    targetTitle: 'Mobile App Development',    time: '5 hours ago',    project: 'Mobile App Development',  },]const actionIcons = {  completed: 'checkCircle',  commented: 'messageSquare',  assigned: 'userPlus',  created: 'plusCircle',  updated: 'edit',  deleted: 'trash2',}export function ActivityFeed() {  return (    <Card className="col-span-3">      Recent Activity</CardTitle>        Latest updates from your team</CardDescription>      </CardHeader>      <CardContent className="space-y-6">        {activities.map((activity) => {          const ActionIcon = Icons[actionIcons[activity.action.activity          return (            <div key={activity.id} className="flex items-start gap-3">              <Avatar className="h-9 w-9">                <AvatarImage src={activity.user.avatar} alt={activity.user.name} />                {activity.user.name.substring(0, 2).toUpperCase()}</AvatarFallback>              </Avatar>              <div className="flex-1">                <div className="flex items-center gap-2">                  <span className="font-medium">{activity.user.name}</span>                  <span className="text-muted-foreground text-sm">                    {activity.action} {activity.target} "{activity.targetTitle}"                    {activity.action === 'assigned' && ` to ${activity.assignedTo}`}                  </span>                </div>                {activity.comment && (                  <div className="mt-2 rounded-lg bg-muted/50 p-3 text-sm">                    {activity.comment}                  </div>                )}                <div className="mt-1 flex items-center gap-2 text-xs text-muted-foreground">                  {activity.time}</span>                  <span>•</span>                  {activity.project}</span>                </div>              </div>              <div className="rounded-full bg-primary/10 p-2 text-primary">                <ActionIcon className="h-4 w-4" />              </div>            </div>          )        })}      </CardContent>    </Card>  )}